# pastor

```
Experiment on minimal and code-first approach to declarative gitops cluster configuration 
```

```shell
# Create example files in the current directory (should be a repo root)
go run github.com/themakers/pastor/cmd/github_bootstrap@latest
```
